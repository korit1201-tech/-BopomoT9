# 更新日誌 (Changelog)

本專案遵循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/) 格式，並採用 [語意化版本 (Semantic Versioning)](https://semver.org/lang/zh-TW/)。

---

## [v1.7.0] - 2026-09-25

### ⌨️ 注音全鍵盤退位與刪除鍵
- **退格鍵、換行鍵與清空鍵配置**：
  - 第 1 排最右側保留專用退格鍵 `⌫`，完美契合電腦實體大千注音鍵盤習慣，支援長按連續退格。
  - 第 4 排右側配置為專用換行/確認鍵 `↵`（組字時一鍵確認上屏首選字，無組字時正常換行）。
  - 第 4 排左側配置「清空」鍵，一鍵清除所有未上屏注音符號。

### 🚀 注音全鍵盤專屬預測與簡打（偷懶輸入）引擎
- **100% 精確音節切分與首碼索引**：
  - 基於教育部標準 429 個合法音節實作精確回溯切分，全量 133,985 筆詞彙 100% 精確抽取首碼構建索引。
- **全方位簡拼與混合偷懶輸入**：
  - **純首碼簡拼**：輸入 `ㄐㄊ` 秒出「今天」、`ㄋㄏ` 出「你好」、`ㄅㄓㄉ` 出「不知道」、`ㄨㄇ` 出「我們」、`ㄊㄨ` 出「台灣」。
  - **混合簡拼**：輸入 `ㄐㄧㄣㄊ` 智能匹配詞首音節 + 後續聲母，精準命中「今天」。
  - **全拼前綴與單字**：支援單字音節、聲調與詞首前綴聯想。
  - **徹底隔離 9 鍵與全鍵盤**：全鍵盤不再轉譯為 9 鍵模糊序列，完全獨立運作。

### 🔀 鍵盤格局重構
- **空白鍵三態滑動切換**：左右滑動在「中文 ↔ 英文(26鍵) ↔ 手寫」直覺循環切換。
- **右下角萬用樞紐鍵**：中文模式切換「9鍵 ↔ 41鍵」（長按繁簡）；英文模式切換大小寫三態。
- **數字模式括號專用鍵**：專用 `( )` 鍵，支援左滑 `(`、右滑 `)`、單擊直接輸出 `()` 並將游標置中。
- **候選列常駐特殊符號/方向鍵盤抽屜按鈕**：全形、半形、數學與方向鍵盤（▲▼◀▶、Home、End、全選、複製、貼上）。

---

## [v1.6.8] - 2026-09-25

### 🔠 9 鍵同鍵位全字組合查詢與個人詞彙最高優先權
- **長按底線候選字同鍵組合完整展開**：
  - 支援依據該單字之 9 鍵序列展開所有同按鍵構成組合之字（例如第 6 鍵長按候選字時，同時完整列出「此、次、意、吃、一、衣、依、醫」等所有同鍵拼法）。
  - 同時包含前綴按鍵延伸單字，候選字量擴充至 80 字。
- **個人選定字詞絕對首選權重（Trie 字典即時注入）**：
  - 使用者選定修改的字或整組確認詞彙，賦予絕對優選增幅，並保證 `userDict.getBoost > 0` 絕對置頂。
  - 確認上屏時直接將新詞/新字以最高權重 (50,000,000) 即時動態注入 Trie 字典，確保後續再次輸入該按鍵序列時 100% 穩居首位候選。

### ⌨️ 數字 9 鍵模式空白鍵優化
- **移除歷史殘留的大寫切換**：
  - 數字 9 鍵模式（123/符號）下的空白鍵固定標示為「空格」，左右滑動直接輸出空白字元，不再誤切換為「大寫」。

### 🔄 App 內版本更新檢查相容性增強
- 優化版本號語意化比對邏輯，完整支援正規版本號比對。

---

## [v1.6.6] - 2026-09-25

### 🔤 文字輸入區底線候選字長按選取同音替換（個人化優選詞庫）
- **正確操作體驗**：直接在目標 App 文字輸入區內長按正在組字的底線候選字（例如「今」或「天」）。
  - 輸入法候選字列即時切換為該字的 **同音/同拼法候選字列表**（依個人使用頻率與詞庫權重排序）。
  - 首項提供「✔ 原字」快速取消/保持原字。
- **支援多字依序改選**：
  - 選定替換字後，輸入框維持底線未提交狀態（例如「今」改「金」後，輸入框顯示為 <u>金天</u>）。
  - 使用者可接著長按第二個字「天」改選為「添」，達到 <u>金添</u>。
- **確認上屏才寫入個人詞庫優選**：
  - 當使用者按下候選字、空白鍵或 Enter 鍵正式確認上屏輸出後，系統才將整詞以及個別替換字寫入本機個人詞庫（`recordUsage`），建立優選優先度。

---

## [v1.6.5] - 2026-09-25

### 🔒 連續輸入前綴保護：防止「今天」被後續注音干擾
- **問題根因**：打完「今天」的注音（ㄐㄧㄣㄊㄧㄢ）並繼續打「天氣」的 ㄊ 時，引擎會將所有按鍵合并做 DP 分詞，有機率將「今天」切割成奇怪的組合。
- **解決方式（滑動視窗自動提交門檻優化）**：
  - 觸發條件從 `≥8鍵` 降為 **`≥6鍵`**（約 2~3 字以上開始生效）
  - 剩餘按鍵門檻從 `≥5鍵` 降為 **`≥3鍵`**（後面只需還有 1 個字就觸發）
  - 效果：「今天」(約6鍵) 加上 ㄊ(1鍵) 共7鍵，DP 分出「今天 + ㄊ...」時，「今天」立即自動上屏鎖定，不再參與後續組字。

---

## [v1.6.4] - 2026-09-24


### 🔍 候選字溢出指示器：長列表滑動提示
- **候選字右滑視覺提示 (Overflow Indicator)**：
  - 在候選字滾動列（`candidate_scroll`）右側加入淡出漸變陰影與溢出箭頭指示器（`›`）。
  - 當候選字數量多且超出螢幕寬度時自動顯現，點擊指示器可直接向右平滑滾動，滾動至最右端時自動隱藏，讓使用者一眼看明白右邊還有更多候選字。

### ⌨️ 空白鍵版面重構：左右分翼與空格標識
- **重構「繁/簡」空白鍵版面**：
  - 將「手寫」與「簡體」提示移至**左右兩翼**（`‹ 手寫   繁   簡體 ›`），中央保持大號粗體主字，空間寬鬆開闊，告別上下擠壓。
  - 下方新增專屬空白鍵標誌（`␣ 空白`），清晰指引點擊即為空白鍵輸入。
  - 滑動切換方向與畫面提示 100% 對齊：左滑直接進入手寫，右滑切換簡繁。

### 🔢 數字鍵盤符號與佈局優化
- **對調 10 鍵與 12 鍵位置**：
  - 將右下角 12 鍵改為最慣用的小數點（`.`），左下角 10 鍵改為（`#`），右手單手打小數點更符合計算機人體工學。
- **數字模式移除全形標點**：
  - 數字鍵盤下的底部按鍵自動切換為半形千分位逗號（`,`）與時間/比例冒號（`:`），不再出全形中文「，」與「。」。

---

## [v1.6.3] - 2026-09-24

### 🖋️ 符號優化：高頻頓號「、」一級置頂
- **替換低頻「～」為繁體常用頓號「、」**：
  - 注音鍵盤與手寫模式的左側第 5 顆標點鍵全面改為繁體中文排比高頻的頓號「、」，長按仍可快捷選取「～」、「·」、「《」、「》」。
  - 完美搭配底部的「，」與「。」，繁體標點輸入更加行雲流水。

### 🚀 長句連續輸入：滑動窗口前綴自動確認 (Sliding Window Prefix Commit)
- **長句輸入前方穩定詞自動上屏**：
  - 導入先進的滑動窗口確認機制：當連續輸入字數/按鍵較多（>= 8 鍵，約 3~4 字以上），且前方分詞已完全確定時（如輸入「今天天氣...」），系統自動將最前面已穩固成詞的「今天」提前上屏確認，留存後續按鍵繼續拼音。
  - 大幅減少長句 DP 計算負擔，字隨鍵走、隨打隨出，輸入體驗極致滑順。

### 🧠 個人詞庫機制重構：淘汰演算法與合理加權
- **容量上限與 LRU/頻率衰減淘汰**：
  - 設定上限 500 條，導入「使用頻率 + 時間衰減」淘汰機制，杜絕無限制膨脹。
  - 專屬詞彙統計（`getEntryCount()`）過濾掉僅選過 1 次的單字，真正反映實際「常用專屬詞彙」數量。
- **漸進式溫和加權飽和曲線**：
  - 徹底移除先前 600 萬的暴力加成，改為單字 +15,000 (上限 60,000)、多字詞 +25,000 (上限 100,000)。
  - 常用詞自然前移超越一般同音詞，同時杜絕冷門字手滑一次便永久霸榜的困擾。

---

## [v1.6.2] - 2026-09-24

### 🎯 徹底修復藍色首選推薦詞怪異問題 (Prediction & Candidate Ranking Optimization)
- **校正階梯式候選字嚴格優先級**：
  - 當按鍵序列有字典完全匹配詞 (`rankedExact`) 時，真實高頻詞（如單字、標準詞彙）永遠穩居第 1 名，藍色首選字忠實回歸使用者最想輸入的真實高頻詞。
  - 只有在字典無完全匹配詞、使用者連續輸入較長句子時，DP 長句分詞句子才進行首選置頂。
- **杜絕未完成按鍵的拼湊怪詞**：
  - 移除先前後綴未完按鍵硬湊單字（如「間喔」）的粗糙保底。DP 長句分詞僅在按鍵序列完整閉合且詞數大於 1 (`words.size > 1`) 時才輸出長句預測，打到一半時自然呈現高頻前綴候選詞，乾淨順暢。

---

## [v1.6.1] - 2026-09-24

### 🐛 修復候選字與接續詞預測消失、高頻字丟失問題 (View Pool Bugfix)
- **根除 ViewPool 元件脫離 (Detached View) 致命 Bug**：
  - 徹底修復先前版本在退格鍵 (`handleBackspace`)、收起鍵盤 (`onFinishInputView`) 或清除畫布時調用 `removeAllViews()` 導致池內 TextView 被系統移除、後續按鍵取出池中 View 卻未重新加入容器而導致高頻候選字（如「見」、「間」）與接續詞預測在畫面上完全隱形消失的重大問題。
  - 新增 `clearCandidateBar()` 統一隱藏候選視圖，並在 ViewPool 取出元件時嚴格校驗 Parent Container 狀態，自動重新掛載，確保候選字與預測詞 100% 穩定呈現。
  - 鍵盤重載 (`onCreateInputView`) 時重置 ViewPool，杜絕舊 Layout 殘留引用。

### 🎹 全面修復「ㄐㄧㄢ 沒有 見」與多字詞注音組合遺失問題 (Syllable & Combo Fix)
- **多字詞注音前綴組合完整回歸**：
  - 左側注音組合欄改為從當前查詢出的候選詞列表提取前綴讀音走向，並結合合法單音節管理器，完美兼顧單字音節（如 `ㄐㄧㄢ`）與多字詞前綴組合（如 `ㄐㄧㄓ` 機制、`ㄍㄣㄓ` 跟著、`ㄍㄣㄗ` 影子等）。
  - 使用者點選任意組合即可精準過濾該讀音下的所有候選字詞，徹底解決「很多組合的字找不到」的問題。
- **大千實體鍵盤聲調精確映射**：
  - 在 `ZhuyinT9Engine` 新增 `setTone(toneChar: Char)` 精確聲調設置方法。
  - 大千實體鍵盤的 3、4、6、7 聲調鍵按鍵由先前錯誤的 `cycleTone()` 修正為精確聲調映射。按下 4 鍵（ˋ，四聲）時立刻鎖定四聲，輸入 ㄐ(R)+ㄧ(U)+ㄢ(0)+4(ˋ) 首選字即為「見」，支援同鍵再次按下解除聲調 (Toggle)。

---

## [v1.6.0] - 2026-09-24

### 🎵 導入台灣注音標準音節圖與合法音節狀態機 (Syllable Parser & Lattice)
- **教育部 429 個標準無聲調音節完整支援**：全面引進 `SyllableManager`，根據注音語法結構（聲母、介音、韻母）維護完整合法音節表，徹底廢除先前粗暴以字元長度 `substring` 截斷拼音的作法，根除多字詞前綴被切成殘缺半個音節（如「ㄒㄧㄤㄍ」）的怪異亂碼。
- **音節歷史頻率累計排序**：字典載入時自動統計 429 個合法音節在真實單字庫中的總詞頻熱度（如 `ㄅㄨ` 13.3萬、`ㄉㄤ` 5.2萬、`ㄉㄨ` 4.8萬、`ㄅㄤ` 2.1萬），為音節檢索提供客觀、合乎台灣語言習慣的權重排序。

### 🍊 重構左側音節選擇器 (Left Syllable Picker Architecture)
- **正向按鍵精確反查音節**：左側橘色按鈕改為依據使用者當前按下的按鍵，直接透過音節狀態機正向檢索所有合法單音節，精確過濾不合法拼音。
- **徹底修復「ㄅㄤ」被擠出與遺漏問題**：
  - 解除先前 `size >= 4 break` 造成的硬性截斷，允許所有合法音節完整排列於縱向捲動視圖中。
  - 輸入鍵 1+9 時，正統音節 `ㄅㄨ`、`ㄉㄤ`、`ㄉㄨ`、`ㄅㄤ` 依照歷史頻率順暢呈現，「幫」所屬之 `ㄅㄤ` 絕不再被腰斬遺漏。
- **支援點擊音節切換解鎖 (Toggle Off)**：點擊左側已鎖定的音節按鈕，支援再次點擊解除鎖定，立即恢復全候選字瀏覽。

### 🧩 重構連續長句輸入與分詞演算法 (Continuous Viterbi DP with Prefix Fallback)
- **回歸標準 Unigram 對數機率模型**：
  - 修正先前將正整數詞頻取 log 後進行正數累加、導致「單字切得越碎正數總分越高」的根本數學謬誤。
  - 改用標準負數對數機率 $\ln(P(w)) \le 0$ 與成詞長度偏置（Word Bonus），優先保護 2 字詞與多字長詞之連續性，分詞準確度迎來質的飛躍（例如「你好」、「今天天氣很好」、「我想吃牛肉麵」等長句不再被碎切成「額此偶偶」、「今天天其小偶」、「我想車有日偶年」等無意義單字）。
- **新增前綴回退保底機制 (Prefix Fallback)**：
  - 徹底解決打長句時因結尾單字尚未輸入完整導致 DP 斷掉、候選字瞬間「全空卡死」的問題。
  - 當整句 DP 無法閉合至最後一個鍵時，演算法自動回退尋找最大有效切點 $j < n$，將前 $j$ 鍵的最佳長句與末尾按鍵之單字候選無縫拼接呈現，輸入永遠流暢有字。

### 🛡️ 容錯詞分級與原生按鍵排序優先權校正 (Tolerance De-prioritization)
- **原生匹配絕對優先**：在 `DictEntry` 引入容錯標記 `isTolerant`。原生按鍵完全吻合的字詞永遠排在最前列；跨鍵位容錯產生的候選詞（如 ㄢ/ㄤ、ㄣ/ㄥ）作為後備補充，排序權重大幅降低並置於原生候選詞之後。
- **杜絕容錯喧賓奪主**：徹底修復按鍵 1+9 時，「但」(ㄉㄢˋ)、「版」(ㄅㄢˇ) 依靠容錯高分強行搶佔排在「幫」(ㄅㄤ)、「邦」(ㄅㄤ) 前方並佔據首選的錯誤現象。

### 🚀 UI 層 View Pool 物件池重用（零記憶體抖動、輸入極致滑順）
- **根除每次按鍵銷毀與動態建立 30 個 View 的性能瓶頸**：
  - 候選字列引入 `candidateTextViewPool`，左側按鈕引入 `comboButtonPool`。
  - 每次按鍵與換字時，直接重用池內現有 View 並僅更新文字與顯示狀態，徹底告別 `removeAllViews()` 與 `new TextView`。
  - 消除按鍵時頻繁觸發的 Dalvik/ART GC 暫停與大量 `requestLayout`，徹底解決打字卡頓、掉幀與不流暢感。

---

## [v1.5.1] - 2026-09-23

### 🐛 修復退格鍵無法刪除已確認上屏文字問題 (Fixed Backspace Unable to Delete Committed Text)
- **根除硬體按鍵事件失效問題**：先前在文字確認上屏（非組字狀態）時，退格鍵使用裸 `ic.sendKeyEvent(KeyEvent.KEYCODE_DEL)`，導致在 WebView、Chrome、Compose TextField、社群通訊軟體等現代 Android 應用中，軟鍵盤的 DEL 事件直接被忽略而無法退格刪除文字。
- **導入標準 Android IME 退格規範**：
  - 支援反白選取文字時直接清除選取區間（`ic.commitText("", 1)`）。
  - 無選取時優先調用 `deleteSurroundingTextInCodePoints(1, 0)` 進行精準刪除，完整保護 Emoji 及 Unicode 補充字元不被拆散。
  - 保留 `deleteSurroundingText(1, 0)` 與標準 `sendDownUpKeyEvents(KeyEvent.KEYCODE_DEL)` 作為雙重備用容錯機制。
- **徹底清除 Composing 狀態殘留**：在選字上屏、空白確認與輸入切換後統一調用 `finishComposingText()`，確保輸入編輯器不會殘留 0 長度的組字區間而阻礙文字退格。

---

## [v1.5.0] - 2026-09-23

### 🌐 「繁」按鍵滑動導引視覺設計 (Visual Swipe Indicator for Traditional/Simplified/Handwriting)
- **常駐滑動指示排版**：空白鍵在注音模式下採用雙行排版，第一行呈現目前主輸入模式（`繁`／`簡`／`手`，加粗大字），第二行以精巧柔和灰色標註滑動指引（`‹ 簡體 · 手寫 ›`），清晰明確提示使用者「向左或向右滑動即可切換至簡體或手寫」。
- **動態十字浮層提示**：滑動空白鍵時，上方四向指示盤同步彈出「簡體」與「手寫」目標標籤，操作反饋極具指引性。

### ⚙️ 設定入口改由「中/EN」長按直達並新增齒輪 ⚙ 視覺標示 (Lang Key Long-press to Settings)
- **直覺齒輪標籤**：語言切換按鈕整合 ⚙ 齒輪圖示（`中/EN\n⚙`），國際標準設計語言讓使用者一目了然「點擊切換中英、長按開啟設定」。
- **邏輯調整**：長按開啟「設定與匯入/匯出」頁面由原先的 123 鍵遷移至中/EN 鍵，123 鍵回歸單純的數字與注音模式點擊切換。

### 🚀 啟動性能暴增與 OOM 徹底根絕（秒開鍵盤，消除十幾秒延遲）
- **詞庫背景非同步載入（Async Loading）**：將 5.2MB、18 萬條官方注音詞庫載入全面移出主線程至背景線程。輸入法初始化與鍵盤視窗彈出耗時從 15 秒驟降至 **< 10 毫秒（0.01 秒）**，切換輸入法**瞬間彈出、零卡頓**，徹底解決先前首次啟動需等待十幾秒甚至被系統殺死無跳出的問題。
- **Trie 樹演算法與記憶體重構（佔用狂降 90%）**：
  - 先前對 18 萬條詞庫每條執行 13 種發音替換，產生上千萬個字串物件，觸發 `java.lang.OutOfMemoryError`（突破 256MB Heap 崩潰）。
  - 現改為標準序列直接入樹，僅針對常用詞（$\le 3$ 字且高頻）進行跨鍵位（3與9、6與12）容錯索引；同鍵位符號（如 ㄓ/ㄗ 均在 Key 3）天然共用，零冗餘記憶體消耗。
  - 記憶體佔用從 >260MB 劇降至 **30MB**，詞庫建構時間由 15 秒縮減至 **0.3 秒**（提速 50 倍）。
- **啟用大記憶體支援**：`AndroidManifest.xml` 開啟 `android:largeHeap="true"`，徹底杜絕極端情況下的 OOM 風險。

### ⚡ 徹底修復快速打字時按鍵順序顛倒問題 (Zero-latency Touch Dispatch)
- **根除 90ms 動畫異步延遲**：先前按鍵浮動預覽關閉時將輸入回呼置於 `view.animate().withEndAction` 中，導致按鍵抬起後被強制延後 90 毫秒才執行。當使用者快速連打時（例如輸入「ㄊㄧㄢ」），後一個未彈窗的按鍵立即執行，導致順序顛倒為「ㄧㄊㄢ」。
- **Touch UP 零毫秒立即同步派發**：改為在手指離開按鍵（`ACTION_UP`）瞬間，第 0 毫秒立即同步派發點擊與滑動事件至注音引擎，視覺動畫改為獨立背景執行，確保按鍵順序 100% 嚴格符合觸控順序。
- **手勢門檻與防誤觸優化**：
  - 長按指南針延遲由 100ms 提高至 220ms，正常快速敲擊（<220ms）絕不觸發彈窗，零 View 負擔。
  - 滑動拖選判定閾值由 16dp 提高至 26dp，徹底杜絕快速敲擊時因手指肉墊形變而誤判為滑動拖選。

### 🧭 四向十字羅盤拖選手勢與視覺動畫 (Compass Drag-to-Select)
- **長拉拖動視覺動畫 (Visual Compass Animation)**：
  - 12 鍵按鍵在長按或滑動時，上方即時彈出精美「四向十字指示盤」浮動氣泡。
  - 手指拖向上、下、左、右時，目標方向字元**即時放大 1.35x 並亮起高對比藍色圓圈，字體純白反光**，其餘方向平滑淡化，放手瞬間伴隨彈性動畫精準上屏。
  - 按鍵本體加入輕微彈性下凹（Scale 0.96x）與抬起回彈微動畫。
- **拖選手勢邏輯全面重構**：
  - 徹底移除先前按住超過 350ms 彈出 `PopupMenu` 搶焦點並吞掉 `ACTION_UP` 拖曳放開事件的重大阻礙，拖選（Drag-to-select）手勢流暢度大幅提升。
  - **數字模式拖選全字母覆蓋**：上(UP)為數字（第 7 鍵為 `s`、第 9 鍵為 `z`），左(LEFT)、下(DOWN)、右(RIGHT)為對應字母/符號，原地短按維持點擊（數字優先）。
  - **注音模式拖選直出**：所有 12 鍵位之注音符號（如 ㄅ, ㄉ, ㄚ, ㄦ 等）完全支援四向拖選直出。

### 🔢 數字 9 宮格主次視覺優化 (Enhanced Number Keypad Aesthetics)
- **主數字加大加粗顯眼**：將 12 鍵數字模式（NUMBER_SYM 與 9 鍵英文）按鍵上的主數字與主符號（如 1~9, 0, ., #）放大 1.45 倍並套用粗體（Bold）與高對比深色，經典電話鍵盤排版主次分明。
- **次要字母與符號優雅縮小**：次要英文字母（a b c 等）與輔助符號縮小至 0.68 倍並採用柔和灰階，換行居中陳列於主數字下方，解決以往字級混雜、數字不夠突出的問題。
- **注音與數字無縫切換**：切換回注音模式自動還原注音字元佈局，字體樣式完全隔離，保持極速與美感。

### 📚 引入 libchewing 官方 16 萬完整詞庫 (libchewing Data Integration)
- **社群高品質詞庫全面替換**：
  - 全面捨棄自建或零散爬蟲字典，直接導入開源知名的 **libchewing-data**（新酷音）官方維護之完整繁體中文詞庫（`tsi.csv` 詞組 + `word.csv` 字表）。
  - 內建 160,000+ 筆台灣地道日常詞條與 26,000+ 筆單字，詞頻客觀、選詞標準、覆蓋全台生活語境。
  - 健全「零韻母口語容錯」（如台灣常見之 `ㄕㄜㄇㄜ` = `什麼`、`ㄓㄜㄇㄜ` = `這麼` 完整支援）。

### 🎯 階梯式候選詞排序機制 (Tiered Candidate Ranking)
- **徹底終結「第一位藍色候選卡在奇怪長詞」**：
  - **Tier 1 (完全匹配)**：長度完全符合當前鍵位之單字或詞彙。使用者個人詞庫常選字詞依點擊次數排前，**常選字詞絕對穩居第 1 位藍色高亮**！
  - **Tier 2 (一口氣長句中文預測)**：一口氣輸入 $\ge 4$ 鍵時，由 DP 全域最佳分詞合成中文句子。若當前無完全匹配詞，預測中文句**直接置頂排在第 1 位藍色高亮**，輸入框中的 Composing 預覽亦即時同步呈現中文，按空格一鍵上屏中文！
  - **Tier 3 (前綴預測詞)**：尚未打完後續鍵位之延伸長詞，永遠排在完全匹配字詞與中文句子之後，絕不可能跨層級搶占藍色候選位。

### 💡 接續聯想詞庫升級與零重複讀取 (Smart Next-word & Zero Disk Thrashing)
- **libchewing 真實詞頻接續詞**：
  - 移除硬編碼的 30 條臨時詞彙，改在字典載入時單次遍歷於記憶體中構建 `nextWordMap` 索引，選字上屏後毫秒級 $O(1)$ 推薦最自然接續詞。
- **杜絕重複讀檔與 IO 卡頓**：
  - 優化 `UserDictionaryManager` 為 2.5 秒防抖延遲合併寫檔機制，徹底消除打字選字過程中的頻繁磁碟 IO 與 Trie 重複重建。

### 📳 按鍵觸控震動開關與強度調整滑桿 (Haptic Tuning)
- **脫離不可靠的系統全域開關**：
  - 廢棄容易在不同 Android 機型上失效的 `Settings.System.HAPTIC_FEEDBACK_ENABLED` 依賴，改為輸入法獨立專屬震動控制。
- **App 設定頁面全新面板**：
  - **按鍵觸覺震動開關 (Switch)**：一鍵開啟或關閉震動反饋。
  - **震動強度調整滑桿 (SeekBar，5 ms ~ 100 ms)**：可精細自訂震動持續時間與振幅，支援**邊滑動邊即時試聽震動強度**。

### 🎨 專屬全新 App Icon (App Icon Design)
- **經典 2×2 大按鍵圖示**：依據真實鍵盤左上角四鍵（ㄅㄉㄚ／ㄍㄐㄞ／ㄆㄊㄛ／ㄎㄑㄟ）設計高質感圖示，生成全套 Android 規格圖示（mdpi、hdpi、xhdpi、xxhdpi、xxxhdpi 及圓形圖示 `ic_launcher_round`），擺脫系統預設小機器人。

### 👤 作者資訊與專案主頁連結 (Author Info & Repository Link)
- **設定頁面完整整合**：在設定頁面明確標註作者資訊（`korit1201-tech`）以及 GitHub 專案主頁連結，使用者點擊即可直接於瀏覽器開啟專案倉庫。
- **詳細版本號識別**：設定頁面顯示詳細版號與建置代碼（`v1.5.0 (Build 8)`）。

### 🚀 應用內線上檢查更新 (In-App GitHub Release Updater - 方案 A)
- **手動檢查更新面板**：設定頁面新增「版本資訊與線上更新」卡片，點擊「🔍 檢查 GitHub 新版本」即時非同步查詢 GitHub Releases 最新發布。
- **應用內一鍵下載並安裝**：偵測到新版本時彈窗展示 Release Notes 與檔案大小，點擊「立即下載並更新」後由 `DownloadManager` 背景下載 APK，完成後透過 `FileProvider` 自動喚起系統安裝器無縫覆蓋更新。

### ⌨️ 版面與鍵位人體工學調優 (Layout Ergonomics)
- **英文 26 鍵 QWERTY**：移除多餘的「9鍵」切換鍵，空白鍵還原為純輸入空格。
- **符號與換行配置**：注音模式下原本右側 `@` 位置轉為換行鍵 `↵`；清空鍵在數字混合模式下轉為換行鍵。
- **中英切換順序**：中文切換英文時以 26 鍵 QWERTY 優先顯示；英文 9 鍵與數字盤整合，數字優先、連按出英文。

---

## [v1.3.1] - 2026-09-22

### 🖊️ 手寫模式改善 (Handwriting UX)
- **新增退格刪字鍵**：手寫模式下底部列「中/英」位置改為 `⌫` 退格鍵，選字後可直接退位刪除，不需切換回其他模式
- **長按連續退格**：按住 `⌫` 超過 400ms 自動進入連續退格（間隔 60ms），與其他模式退格行為一致
- **畫布不再被遮擋**：`⌫` 移至底部功能列，手寫畫布全區域可用
- **模式切換自動還原**：離開手寫模式時，「中/英」位置自動恢復語言切換功能

---

## [v1.3.0] - 2026-09-22

### 🧠 個人化記憶與詞庫匯出/匯入 (Personalization & Backup)
- **本機個人詞庫動態學習 (Frequency Learning)**：
  - 每次打字點選候選詞或按空白鍵確認時，自動記錄該詞的使用次數與最新時間。
  - 依據個人使用次數即時給予候選詞高額動態權重（每選 1 次 +6,000,000 分），越常選的詞自動晉升至第 1 候選位！
  - 100% 本地離線儲存於手機端（JSON 檔案），零聯網上傳，絕對保障個人隱私安全。
- **自訂詞庫完整匯出與匯入 (Export & Import)**：
  - 在 App 主設定頁面新增「個人化詞庫管理」面板，即時查看目前已學習的詞彙統計。
  - **📤 匯出詞庫**：支援透過 Android Storage Access Framework (SAF) 匯出為標準 TSV 純文字檔（`bopomofo_user_dict.txt`），可隨時備份至 Google Drive、SD 卡或傳到新手機。
  - **📥 匯入詞庫**：可挑選任何自訂詞庫文字檔自動解析合併，支援包含注音與頻率格式，或純詞彙文字檔。
  - **🗑️ 清空紀錄**：提供二次確認防誤觸的詞庫重設功能。
- **鍵盤快捷直達**：
  - 鍵盤底部「123」鍵支援**長按快速開啟**設定與詞庫管理頁面，無需退回手機桌面找 App。

---

## [v1.2.0] - 2026-09-22

### 🎯 輸入精準度大幅躍升 (Accuracy & Engine)
- **觸控防誤觸機制全面升級**：
  - 改採動態 DP 比例（28dp，約 75~85px）滑動門檻，杜絕高解析度螢幕快速連打時拇指微動被誤判為滑動（Swipe）噴出孤立注音符號的嚴重問題。
  - 增加主軸與次軸位移比（需大於 1.3 倍）及最小按壓時間門檻（80ms），斜向滑動與超速點擊一律保證判定為精準單擊（Tap）。
- **詞庫品質重構與高頻詞權重校準**：
  - 清理詞庫中 298 筆人為錯誤生成的「假單音節重複詞」（如 `午安` 誤標為 `ㄨㄢ` 導致覆蓋 `晚/玩/萬` 的嚴重 Bug）。
  - 新增並補齊「台灣」、「今天」、「明天」、「晚上」、「學校」、「工作」、「吃飯」等上百個台灣核心日常高頻詞彙（權重提升至 9,000,000+）。
  - 對核心單字（`的`、`是`、`在`、`有`、`我`、`你`、`他`、`了`、`就`、`也`、`這`、`要`、`說` 等）提供頂級權重加護，確保打單字時第一候選直覺精準。
- **動態規劃 (DP) 全域最佳分詞**：
  - 廢棄傳統脆弱的貪婪比對，改採動態規劃求得全句最大機率切詞，大幅降低長句斷詞錯誤。
  - 當輸入序列在字典中已有完整詞匹配時（如「台灣」），完整詞優先保留於首位，避免被切詞強行拆散。
- **發音容錯權重微調**：
  - 容錯詞（ㄣ/ㄥ、ㄢ/ㄤ等）權重折減由 0.85 調整為 0.55，嚴格確保標準發音優先排在最前列。

---

## [v1.1.0] - 2026-09-22

### ✨ 新增功能 (Added)
- **數字模式計算機符號列**：
  - 12 鍵數字模式左側欄直出常用運算符號 `+` `-` `*` `/` `=`。
  - 支援長按展開延伸數學符號選單（`±`、`—`、`×`、`÷`、`≠`、`≤`、`≥`）。
  - 中央數字鍵第 10 鍵升級為小數點 `.`，與 `0`、`#` 並列底部，便於金額與算式輸入。
- **英文 26 鍵（QWERTY）專屬符號列**：
  - 頂部直接整合常用符號列 `+` `-` `*` `/` `=` `(` `)` `@` `_` `&`，單擊直出。
  - 符號鍵長按呼出完整家族（例如 `(` 呼出 `[` `{` `<` 等）。
  - 字母鍵支援長按快捷輸入數字與常用標點。
- **實體鍵盤大千鍵位補充**：
  - 加入 `=` 鍵對應二聲（`ˊ`），貼近標準電腦大千注音鍵盤習慣。

### 🐛 問題修復 (Fixed)
- **聲調切換與候選字過濾**：
  - 修復注音切換聲調時的顯示異常，左側注音組合列即時更新帶聲調標籤（如 `ㄅㄚˊ`）。
  - 候選字列嚴格按選定聲調過濾，大幅提升選字效率。
- **WebView / 網頁輸入框退格相容性**：
  - 退格鍵補齊 Android 標準 `ACTION_DOWN` / `ACTION_UP` 成對事件，徹底解決特定網頁或 WebView 內刪除失效的問題。
- **鍵盤模式切換按鍵狀態殘留**：
  - 全面補齊所有模式（QWERTY、手寫、注音、數字、9鍵英文）下的符號列重設與監聽器清理邏輯。
  - 英文 9 鍵模式智慧隱藏右側重複的 `@` 按鍵。
- **記憶體與效能防禦**：
  - 修復 `HandwritingCanvasView` 離開視窗時未移除定時 Handler 可能引發的記憶體洩漏。
  - 前綴樹（Trie）模糊前綴搜索加入嚴格剩餘配額截斷，防止記憶體超額佔用。
  - 移除 `TrieNode` 內部未使用的死代碼欄位。

### ⚡ 效能與建置優化 (Performance & Build)
- 啟用 R8 混淆與資源壓縮（Shrink Resources），Release APK 體積由 47.8 MB 瘦身至 **22.6 MB**。
- 更新 `versionCode` 為 `2`，`versionName` 為 `1.1.0`。

---

## [v1.0.0] - 2026-09-22

### ✨ 初始正式發布 (Initial Release)
- 🎯 **12 鍵（4×3）經典注音九宮格**：
  - 支援滑動（Swipe）直出注音、長按注音精確選單、K11 聲調循環切換。
  - 空白鍵長按快選常用標點（`，` `。` `！` `？` `……` `：`）。
- 🧠 **在地化注音 Trie 引擎與拼寫容錯**：
  - 內建 55,000+ 筆台灣常用詞庫。
  - 智慧發音容錯（ㄣ/ㄥ、ㄢ/ㄤ、ㄓ/ㄗ、ㄔ/ㄘ、ㄕ/ㄙ、ㄧㄣ/ㄧㄥ）。
  - 雙字 N-gram 語意接續聯想推薦詞。
- ✍️ **Google ML Kit 端側手寫辨識**：
  - 整合官方 `Digital Ink Recognition` 神經網路模型（與 Gboard 同源），離線推論、高度準確。
  - 空白鍵左右滑動即時輪替「繁」→「簡」→「手」。
- 📏 **鍵盤高度自由拉伸（Drag-to-Resize）**：
  - 拖曳把手隨時縮放鍵盤高度（180dp ~ 380dp），自動記憶偏好。
- ⌨️ **PC 級實體鍵盤支援**：
  - 支援外接鍵盤標準大千注音鍵位、單按 Shift 切換中英。
- 🔠 **英文 9 鍵（Multi-tap）與 26 鍵（QWERTY）雙輸入**。
- 📳 **系統級觸覺震動回饋**。
